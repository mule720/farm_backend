apiVersion: apps/v1
kind: Deployment
metadata:
  name: agronexus-frontend
  namespace: agronexus
  labels:
    app: agronexus-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agronexus-frontend
  template:
    metadata:
      labels:
        app: agronexus-frontend
    spec:
      containers:
        - name: frontend
          image: us-central1-docker.pkg.dev/agrinexus-farm/agronexus/frontend:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
